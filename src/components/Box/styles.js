import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  containerStyle: {
    paddingVertical: 10,
    // justifyContent: 'center',
    // alignItems: 'center',
    borderRadius: 5,
    marginBottom: 10,
  },
  titleStyle: {
    color: 'white',
    fontWeight: 'bold',
    textAlign: 'center',
    fontSize: 16,
    lineHeight: 24,
  },
});
